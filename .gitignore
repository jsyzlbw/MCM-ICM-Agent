# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Local environment
.env
.env.*
!.env.example
mcm_agent_config.local.json

# Runtime workspaces and generated contest artifacts
workspace/
workspaces/
runs/
final_submission/
knowledge_base/*
!knowledge_base/.gitkeep
*.aux
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.log
*.out
*.synctex.gz

# macOS / editors
.DS_Store
.idea/
.vscode/

# Brainstorming visual companion
.superpowers/

# Embedding cache
.mcm_agent_cache/

# GUI workspaces
.mcm_agent_workspaces/

# Diagnostic / contest copyright material (COMAP problem + data) — never commit
assets/diagnostic_2026_mcm_c/


# Large MCM corpus + built KB (data, not source)
assets/mcm_icm_corpus/          # 4.4G raw award-paper PDFs — never published
# Publish the KB's text + teardown/pattern layers + manifest (~37M); keep out of git:
#   _work/ (MinerU scratch), embedding_cache.db (368M), chroma/ (410M vector DB).
# The vector DB ships as a GitHub Release asset (corpus_kb_chroma.tar.gz), not via git.
corpus_kb/_work/
corpus_kb/embedding_cache.db
corpus_kb/chroma/
