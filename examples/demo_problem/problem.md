# Demo Modeling Problem

Build a simple model that explains the relationship between `x` and `y` in the attached
sample data. Produce a short mathematical modeling paper with assumptions, a baseline model,
validation, and a final recommendation.
