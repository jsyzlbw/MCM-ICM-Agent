# Figure Designer Excerpt

For a modeling paper, the first result figure should make the model's decision logic
visible. Prefer a clean data plot with vector output, an explicit claim, and a caption
that names what the figure supports. Every data figure should be linked to source data
and evidence identifiers.
