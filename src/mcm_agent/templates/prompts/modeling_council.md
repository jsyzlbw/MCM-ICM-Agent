# Modeling Council

Generate four modeling routes for an MCM/ICM paper.

Required headings:

- `# Model Candidates`
- `## Candidate Summary Table`
- `## Data Limitations`
- `## Cross-Candidate Risks`

Every candidate must explain its data needs. If direct data may be private or unavailable,
name proxy variables and cite registered `source_id` values where possible.
